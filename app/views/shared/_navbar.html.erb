<div class="head">
  <h1>Cocktails</h1>
  <div class="toolbar" role="toolbar" aria-label="Toolbar with button groups">
      <% ('a'..'z').to_a.each do |letter| %>
        <button class="btn btn-light"><%= link_to letter, cocktail_index_path({letter: letter}) %></button>
      <% end %>
    </ul>
  </div>
</div>